<div class="container">
  <h1 class="mt-3">Add Review on <em><%= @restaurant.name %></em></h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit class: 'btn btn-primary mt-2' %>
  <% end %>
</div>
